#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include <cmath>



int main() {
    int a = 10;
    int b = 20;
    int c = 30;
    int d = 40;

    std::cout << a << std::endl;
    std::cout << b << std::endl;
    std::cout << c << std::endl;

    std::cout << "a + b + c = " << a + b + c << std::endl;
    std::cout << "a * b * c = " << a * b * c << std::endl;
    std::cout << "a - b - c = " << a - b - c << std::endl;
    std::cout << "a / b / c = " << a / b / c << std::endl;
}
